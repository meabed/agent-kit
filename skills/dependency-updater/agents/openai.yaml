interface:
  display_name: 'Update Dependencies'
  short_description: 'Update dependencies and run repository checks'
  default_prompt: 'Use $dependency-updater to update these packages and run the required checks.'
