interface:
  display_name: 'Remove Trivial Tests'
  short_description: 'Remove shallow tests and keep behavioral coverage'
  default_prompt: 'Use $remove-trivial-tests to audit every test touched by this branch.'
