interface:
  display_name: 'Compress Worklog'
  short_description: 'Shorten work logs without losing important details'
  default_prompt: 'Use $compress-worklog to shorten these notes so another agent can continue the work.'
